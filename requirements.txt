# Install PyTorch first, matching your platform/GPU setup:
#   https://pytorch.org/get-started/locally/
# e.g. CPU-only: pip install torch torchvision
# Then install everything below.

# Pose estimation / computer vision
opencv-contrib-python>=4.9
mediapipe>=0.10
ultralytics>=8.0  # 2D keypoint detector for the VideoPose3D pipeline (COCO format)

# Numerics
numpy>=1.26
scipy>=1.12

# Config / data I/O
pyyaml>=6.0
h5py>=3.10

# Experiment tracking
wandb>=0.16

# Visualization
# vpython 7.6.x imports the deprecated pkg_resources API at import time, which
# recent setuptools versions have dropped entirely - pin below 81 or vpython
# fails to import. Revisit if/when vpython ships a fixed release.
setuptools<81
vpython>=7.6
matplotlib>=3.8

# Web demo (single-camera public Space)
gradio>=5.0

# Dev / testing / tooling
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
tqdm>=4.66
